#include"main.h"

unsigned int _strspn(char *s, char *accept)

{
	unsigned int i;
	unsigned int a;

	{
	for (i = 0; s[i] != '\0'; i++)
	{
		for (a = 0; accept[a] != s[i]; a++ )
			if (accept[a] ==#include"main.h"

