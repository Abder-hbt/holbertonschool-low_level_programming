#include "main.h"
/**
 * mul - Checks for uppercase character
 * @a: The character to be checked
 * @b: multi
 * Return: a if c is uppercase, 0 otherwise.
 */

int mul(int a, int b)
{
	return (a * b);
}
